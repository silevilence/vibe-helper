# 项目开发路线图 (Roadmap)

## 📅 计划中

## 🚧 开发中

## ✅ 已完成

- [x] **项目初始化**
    - [x] 初始化Nodejs项目
        - 配置 TypeScript 运行环境。
        - 实现**智能 JS 桥接策略**（创建 `bin/run.js`）：根据运行环境动态判断，优先执行已编译的 `dist` 产物（面向未来 npm 发布），回退使用 `npx tsx` 执行源码（兼容当前直接从 GitHub 运行），并将其配置为 `package.json` 的 `bin` 入口。
    - [x] 初始化目录结构
        - 建立代码与资源严格分离的标准架构：划分 `src/`（核心逻辑、CLI/TUI 控制）与 `res/tools/init/`（模板片段、基础规范、技能集、配置文件模板）。
    - [x] 安装必要的包
        - 引入核心依赖：`commander`（CLI 路由）、`@clack/prompts`（TUI 交互）、`fs-extra`（文件系统操作）、`tsx`（开发期与 GitHub 源码执行引擎，当前阶段置于 `dependencies` 中）。

- [x] **实现整体界面**
    - [x] 实现功能选择主界面
        - 采用 `@clack/prompts` 构建现代化的终端交互主菜单，包含启动引导、加载动画、优雅的异常处理与退出提示。
    - [x] 建立工具注册架构
        - 基于 `commander` 封装插件化、松耦合的子命令注册架构，预留接口以确保未来能平滑横向扩展（如 `add`、`eject`、`audit` 等工具）。

- [x] **init 工具开发**
    - [x] 注册init工具，名称为项目初始化
    - [x] `init` 工具说明
        - **功能定位**：面向 Vibe Coding 场景的 AI Agent 配置脚手架。通过终端交互采集研发需求，动态读取并无缝拼装底层模板片段，自动化生成规范的工程配置文件。
        - **执行步骤与核心逻辑**：
            1. **采集基础项目信息**：读取当前执行目录名称作为“项目名称”的默认值（支持用户重写）；提供“项目描述”的全文本输入流（支持留空）。
            2. **配置 AI Agent 引擎**：选择核心辅助工具（提供 GitHub Copilot、Crush 选项，支持多选）。
            3. **配置工程技术栈（基于动态级联交互）**：
               - 选取**开发语言**（单选闭环：C# / F# / TypeScript / Rust / C++）。
               - 根据所选语言，**级联触发**交付类型选择（如选取 TypeScript 后，仅展示 CLI / React / Vue / NPM 包，并提供 Electron 组合选项）。
               - 根据所选语言，**条件触发**运行时环境配置（仅选取 C#/F# 时，触发 .NET 8 / 10 版本选择）。
            4. **配置研发工作流与 AI 规范边界**：
               - 配置**测试驱动策略**：TDD / 测试覆盖 / 无测试（默认：测试覆盖。**注意**：若选择“无测试”，必须触发防呆二次确认告警）。
               - 配置**文档与 Git 权限边界**：设定文档更新权限（默认禁止自动更新）与代码提交权限（默认仅限手动提交）。生成的文档中需植入红线规则：**严禁 AI 篡改未经许可的核心文档，严禁 AI 调用 `git commit / push` 写入命令**。
               - 配置**Agent 技能挂载**：程序动态遍历 `res/tools/init/skills` 目录，生成技能列表供用户多选。
            5. **执行安全校验与产物生成**：
               - **文件防篡改/防覆盖保护**：在执行磁盘写入前，必须探测目标路径（如 `.github/copilot-instructions.md`、`ROADMAP.md`）是否存在。若已存在，必须通过 TUI 弹出二次确认交互（覆盖 / 中止），严禁静默覆盖用户资产。
               - **模板动态组装与输出**：基于用户配置树，异步读取对应的局部 Markdown 模板，执行片段拼接（避免代码硬编码）。最终交付：
                 - **5.1 核心指导文件**：生成 `.github/copilot-instructions.md`，融合项目元数据、目录结构树、语言开发规范、质量要求与安全边界限制。
                 - **5.2 技能库**：初始化 `.github/skills/` 目录，并注入所选技能模块。
                 - **5.3 路线图**：同步 `ROADMAP.md` 模板至项目根目录。
                 - **5.4 引擎私有配置**：若启用 Crush 工具，同步 `crush.json` 至项目根目录。

- [x] **扩展 Python 技术栈支持**
    - [x] 集成 Python 工程初始化配置至 `init` 工具模块
    - [x] 增设依赖管理工具配置项（候选：uv / pip / conda，默认值：uv）
        - 基于工具选型，动态映射并注入对应的包管理规范片段至 `copilot-instructions.md`
        - 针对 uv 引擎，定制并写入专属工作流约束（强制规定工程构建、依赖维保及脚本执行等生命周期均须统一调用 uv 标准指令）

- [x] **全局配置 (Config) 模块开发**
    - [x] 注册 Config 子命令主入口，使其在 TUI 主干路由中与 Init 模块并列平级
    - [x] 构建配置状态管理机制：实现参数持久化写入用户本地环境，并保障每次 CLI 唤醒时自动加载至上下文
    - [x] 确立参数缺省与回退策略：建立全局默认配置字典，确保所有缺失项自动回退至标准默认值
    - [x] 定义核心配置项数据模型 (Schema)
        - Init 工具执行策略配置（优先级：高，关联当前核心业务）
            - `copilot-instructions.md` 写入冲突控制：覆盖前二次确认拦截（默认：开启）
            - `ROADMAP.md` 写入冲突控制：覆盖前二次确认拦截（默认：开启）
            - `crush.json` 覆盖模式控制：静默替换 / 覆盖前二次确认 / 结构合并前二次确认（默认：结构合并前二次确认）
        - 大语言模型 (LLM) 引擎网关配置（预留架构扩展点，为后续功能铺垫）
            - 供应商类型枚举 (OpenAI Chat / Gemini / Claude)
            - API 基础请求地址 (Base URL)
            - 模型资源标识 (Model ID)
    - [x] Init 工具相关配置逻辑落地与挂载
        - 桥接配置状态，动态接管 Markdown 文件写入流，按需触发防覆盖的安全拦截交互
        - 解析 `crush.json` 的覆盖策略：实现文件流的直接覆盖、拦截询问流程，以及 JSON AST 级别的深度合并 (Deep Merge) 算法
